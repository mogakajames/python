#chnage the python installation path on top of every script to run
#provide the row text file unaltered on the path input field
#Use the create beegenes table script to create the table
#For connection, change the connection credentials to match your local installation of Oracle Db
#execute the code and it'll run and process the data as expected